## What

<!-- One paragraph. What changes for a reader of the spec, a user of the service, or a user of the generator. -->

## Why

<!-- The problem this solves, or the question it settles. Link the issue if there is one. -->

## Checklist

- [ ] `npm run check` passes (typecheck, tests, schema validation, build)
- [ ] If the spec text changed: the JSON Schema and the reference implementation still agree with it
- [ ] If the renderer or the generator changed: `npm run conformance` / `npm run example` were run and the diff is in this PR
- [ ] No real API, hostname, credential or internal name appears in examples or fixtures
